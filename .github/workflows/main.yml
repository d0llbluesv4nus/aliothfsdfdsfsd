name: Poco F3 ROM Port (Native Linux Tools)

on:
  workflow_dispatch:
    inputs:
      stock_url:
        description: 'STOCK Fastboot ROM (alioth)'
        required: true
      port_url:
        description: 'PORT Fastboot ROM (donor)'
        required: true
      rom_name:
        description: 'ZIP name'
        default: 'Alioth_Port_Release'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # 1. Очистка места
      - name: Free Disk Space
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: true
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: false

      # 2. Установка инструментов
      - name: Install tools
        run: |
          sudo apt-get update
          # Устанавливаем erofs-utils и e2fsprogs (для mke2fs) из репозитория
          sudo apt-get install -y aria2 p7zip-full git file unzip \
            android-sdk-libsparse-utils erofs-utils \
            build-essential liblz4-dev liblzma-dev uuid-dev e2fsprogs

          # 1. lpunpack (LineageOS)
          echo ">>> Installing lpunpack..."
          wget -q https://raw.githubusercontent.com/LineageOS/android_prebuilts_extract-tools/lineage-21.0/linux-x86/bin/lpunpack -O lpunpack
          chmod +x lpunpack && sudo mv lpunpack /usr/local/bin/

          # 2. make_ext4fs (Попытка скачать из надежного источника)
          echo ">>> Installing make_ext4fs..."
          # Ссылка из репозитория бинарников TWRPDtGen
          if wget -q https://github.com/twrpdtgen/binaries/raw/master/linux/x86/make_ext4fs -O make_ext4fs; then
             chmod +x make_ext4fs
             sudo mv make_ext4fs /usr/local/bin/
             echo "✅ make_ext4fs downloaded."
          else
             echo "⚠️ make_ext4fs download failed. Will use mke2fs fallback."
          fi

          # 3. Magiskboot (Официальный APK)
          echo ">>> Installing Magiskboot..."
          wget -q https://github.com/topjohnwu/Magisk/releases/download/v27.0/Magisk-v27.0.apk
          unzip -q Magisk-v27.0.apk -d magisk
          cp magisk/lib/x86_64/libmagiskboot.so magiskboot
          chmod +x magiskboot && sudo mv magiskboot /usr/local/bin/
          rm -rf magisk Magisk-v27.0.apk

          echo "✅ Все инструменты готовы."

      - name: Prepare workspace
        run: mkdir -p workspace/{stock,port,final/images,assets,checks}

      # 3. Обработка STOCK
      - name: Process STOCK
        run: |
          cd workspace/stock
          echo ">>> Скачивание стока..."
          aria2c -x4 -s4 "${{ inputs.stock_url }}" -o stock.tgz
          
          echo ">>> Распаковка tar..."
          tar -xf stock.tgz
          rm stock.tgz

          IMG_DIR=$(find . -type d -name "images" | head -n1)
          if [ -z "$IMG_DIR" ]; then
             IMG_DIR=$(find . -name "boot.img" -printf "%h\n" | head -n1)
          fi
          cd "$IMG_DIR"

          SUPER=$(find . -name "super.img" | head -n1)
          if [ -f "$SUPER" ]; then
             echo ">>> Распаковка super.img..."
             simg2img "$SUPER" super.raw
             lpunpack super.raw .
             rm super.raw
          fi

          # Копирование файлов
          cp boot.img ../../final/images/
          cp dtbo.img ../../final/images/
          cp vbmeta.img ../../final/images/
          
          VENDOR=$(ls -S vendor*.img | head -1)
          cp "$VENDOR" ../../final/images/vendor.img
          
          # Определение FS и извлечение fstab
          PRODUCT=$(ls -S product*.img | head -1)
          
          if fsck.erofs --extract=product_fs "$PRODUCT"; then
             echo "STOCK_FS=erofs" >> $GITHUB_ENV
             echo ">>> Сток использует EROFS"
          else
             echo "STOCK_FS=ext4" >> $GITHUB_ENV
             echo ">>> Сток использует EXT4"
             7z x "$PRODUCT" -oproduct_fs
          fi

          find product_fs -name "fstab.*" -exec cp {} ../../assets/ \; || true
          rm -rf product_fs

      # 4. Обработка PORT
      - name: Process PORT & Repack
        run: |
          cd workspace/port
          echo ">>> Скачивание порта..."
          aria2c -x4 -s4 "${{ inputs.port_url }}" -o port.tgz
          
          tar -xf port.tgz
          rm port.tgz

          IMG_DIR=$(find . -type d -name "images" | head -n1)
          if [ -z "$IMG_DIR" ]; then IMG_DIR=$(find . -name "system.img" -printf "%h\n" | head -n1); fi
          if [ -z "$IMG_DIR" ]; then IMG_DIR=$(find . -name "super.img" -printf "%h\n" | head -n1); fi
          cd "$IMG_DIR"
          
          SUPER=$(find . -name "super.img" | head -n1)
          if [ -f "$SUPER" ]; then
             simg2img "$SUPER" super.raw
             lpunpack super.raw .
             rm super.raw
          fi

          repack() {
            NAME=$1
            if [ "$NAME" == "system" ]; then
               IMG=$(ls -S system.img system_a.img 2>/dev/null | head -1)
            else
               IMG=$(ls -S ${NAME}*.img 2>/dev/null | head -1)
            fi

            if [ -f "$IMG" ]; then
               echo ">>> Processing $NAME ($IMG)..."
               rm -rf extracted && mkdir extracted
               
               # Распаковка
               if ! fsck.erofs --extract=extracted "$IMG" 2>/dev/null; then
                  7z x "$IMG" -oextracted > /dev/null
               fi
               rm "$IMG"

               # Inject fstab
               if [ -f ../../assets/fstab.qcom ]; then
                  echo "   -> Injecting stock fstab..."
                  find extracted -name "fstab.*" -delete
                  cp ../../assets/fstab.* extracted/etc/ 2>/dev/null || true
                  mkdir -p extracted/vendor/etc 2>/dev/null || true
                  cp ../../assets/fstab.* extracted/vendor/etc/ 2>/dev/null || true
               fi

               # Repack
               if [ "${{ env.STOCK_FS }}" = "erofs" ]; then
                  # EROFS REPACK
                  echo "   -> Repacking to EROFS (LZ4)..."
                  mkfs.erofs -zlz4 -C16384 ../../final/images/${NAME}.img extracted
               else
                  # EXT4 REPACK
                  echo "   -> Repacking to EXT4..."
                  
                  # Используем make_ext4fs если есть, иначе mke2fs (Native Linux method)
                  SIZE_MB=$(du -sm extracted | awk '{print $1+150}')
                  SIZE_BYTES=$(($SIZE_MB * 1024 * 1024))
                  
                  if command -v make_ext4fs >/dev/null; then
                      make_ext4fs -s -L $NAME -l ${SIZE_MB}M -a $NAME ../../final/images/${NAME}.img extracted/
                  else
                      echo "      (Using mke2fs fallback)"
                      # 1. Создаем пустой файл нужного размера
                      truncate -s ${SIZE_MB}M raw.img
                      # 2. Форматируем в ext4 и заливаем файлы (-d)
                      # -O ^has_journal (без журнала, как на андроид)
                      # -M /$NAME (точка монтирования)
                      mke2fs -t ext4 -b 4096 -L $NAME -M /$NAME -O ^has_journal -d extracted raw.img
                      # 3. Конвертируем в Sparse (сжатый андроид формат)
                      img2simg raw.img ../../final/images/${NAME}.img
                      rm raw.img
                  fi
               fi
               rm -rf extracted
            fi
          }

          repack system
          repack system_ext
          repack product
          repack mi_ext
          repack odm

      # 5. Патч boot
      - name: Patch boot
        run: |
          cd workspace/final/images
          if [ -f boot.img ]; then
             echo ">>> Patching boot.img..."
             magiskboot unpack boot.img
             if [ -f header ]; then
                magiskboot hexpatch kernel 73656C696E75783D656E666F7263696E67 73656C696E75783D7065726D697373697665
             fi
             magiskboot repack boot.img
             mv new-boot.img boot.img
             rm -f kernel ramdisk.cpio dtb header
          fi

      # 6. Упаковка и релиз
      - name: Pack ZIP
        run: |
          cd workspace/final
          cat <<EOF > flash_all.bat
          @echo off
          fastboot flash boot images/boot.img
          fastboot flash dtbo images/dtbo.img
          fastboot flash vendor images/vendor.img
          if exist images\odm.img fastboot flash odm images/odm.img
          fastboot flash system images/system.img
          fastboot flash system_ext images/system_ext.img
          fastboot flash product images/product.img
          if exist images\mi_ext.img fastboot flash mi_ext images/mi_ext.img
          fastboot reboot
          pause
          EOF

          ZIP="${{ inputs.rom_name }}.zip"
          7z a -tzip -mx1 -v2000m "$ZIP" .
          echo "TAG=v$(date +'%Y.%m.%d-%H%M')" >> $GITHUB_ENV

      - uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ env.TAG }}
          name: Alioth Port ${{ env.TAG }}
          files: workspace/final/${{ inputs.rom_name }}.zip.*
